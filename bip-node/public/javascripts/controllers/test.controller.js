'use strict';
angular.module('myApp')
    .controller('testController',['$rootScope','$scope','userListService','$state',
        function($rootScope,$scope,userListService,$state){
           // $state.go('test.test1');
        }

    ]);
