doctype html
html
    head
        meta(charset="utf-8")
        meta(name="viewport" content="initial-scale=1, maximum-scale=1, user-scalable=no, width=device-width")
        title= title
        link(rel="icon" href="/images/bofideIco.ico" type="image/x-icon")
        link(href='/styles/market.css', rel='stylesheet', type='text/css')
        script(type="text/javascript",src='/components/jquery/dist/jquery.min.js')
        script(type="text/javascript",src='/components/jquery-form/jquery.form.js')
    body
        script.
            $(function () {
                $("#updateCustForm").submit(function () {
                    $(this).ajaxSubmit({
                        success: function (rs) {
                            alert(rs.message);
                        },
                        timeout: 10000
                    })
                    return false;
                })
            })
        .smsboxbg
            .smstop
                .smstitle 活动详情：
                textarea(rows="3" cols="30" class="smsdetails" maxlength="500" readonly)=details
            .smsbottom
                form(id='updateCustForm', action='/marketing/updateCustomer', method='post')
                    input(id='customerId' name='customerId',value=customerId type='hidden')
                    input(id='contactBtn' type='submit' value='感兴趣，联系我' )
                p.storeName=storeName